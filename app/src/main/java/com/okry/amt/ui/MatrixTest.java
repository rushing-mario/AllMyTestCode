package com.okry.amt.ui;

import android.content.Context;
import android.graphics.Canvas;
import android.util.AttributeSet;
import android.widget.ImageView;

/**
 * Created by mr on 15/3/16.
 */
public class MatrixTest extends ImageView {
    public MatrixTest(Context context) {
        super(context);
    }

    public MatrixTest(Context context, AttributeSet attrs) {
        super(context, attrs);
    }

    public MatrixTest(Context context, AttributeSet attrs, int defStyle) {
        super(context, attrs, defStyle);
    }

    @Override
    protected void onDraw(Canvas canvas) {
        super.onDraw(canvas);
    }
}
